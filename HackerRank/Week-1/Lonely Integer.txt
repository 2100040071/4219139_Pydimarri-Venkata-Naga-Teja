#include <stdio.h>
int main(void) 
{
    int n,i;
    scanf("%d", &n);
    int result = 0; 
    for (i = 0; i < n; i++) 
    {
        int num;
        scanf("%d", &num);
        result ^= num; 
    }
    printf("%d\n", result);
    return 0;
}
